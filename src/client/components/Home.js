import React from 'react';

const Home = () => {
  return (
    <div>
      <div>Home VERY BEST component</div>
      <button onClick={() => console.log('Pressed')}>Press me!</button>
    </div>
  );
};

export default Home;